<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="guest">Guest</span>
    <span class="guest-cap">{{format_amount guest_cap}}</span>
    
  </p>
  <p>
    <button class="btn btn-primary">EventFull!</button>
  </p>
</div>
